# Local secrets — never commit
_private/
*.local.md
*.local.txt

# Brand kit archive (large, not for deploy)
drive-download-*.zip

# Build output
dist/
utea-site.zip

# Temp screenshots
preview-*.png
_check.png

# Source cover art dropped in repo root (the deploy copy lives in assets/)
/cover1.png
/cover2.png
/example.htm

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
